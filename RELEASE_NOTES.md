# V0.4.0 更新内容

本版本为**AI智能筛选版**(基于 V0.3.3),新增 AI 辅助筛选功能:

## 新增功能

1. **AI实时判断(爬取页)** —— 勾选"启用AI实时判断"(位于任务参数最后一行,旁有"测试连接"按钮)后,爬取每篇文章时调用AI判断"高质量可信度",低于阈值(默认80%)的文章不导出也不下载媒体;通过的文章会在文件末尾标注 `高质量可信度:xx%`。需要先在"AI筛选"页签配置 API
2. **AI筛选页签(新增第三个页签)**
   - **API设置**: 填写 API Key(可显示/隐藏)、接口地址、模型,支持 DeepSeek/通义/Kimi 等任何 OpenAI 兼容服务,一键"测试连接";密钥保存在程序目录 ai_config.json(默认模型为 deepseek-v4-flash,V4 系列轻量模型,便宜快速)
   - **判断阈值**: 高质量可信度阈值、广告概率阈值、可疑下界,均可自定义
   - **AI提示词**: 分类提示词可编辑,"恢复默认提示词"一键还原(提示词已优化为只返回JSON,节省token)
   - **事后AI分类**: 扫描本地已爬取的文章,逐篇AI判断后按类别复制到 `筛选/AI_博主_高质量|广告|可疑|其他低质量` 文件夹(保留年月目录结构,图片/视频同步复制),**默认保留原文件**(可取消勾选改为移动);支持断点续跑("清空进度"可全部重跑)、实时进度条、token用量统计
   - **AI总结**: 可选生成标题总结,写入各类别目录的 AI总结.txt;默认**不保留原标题**,文件名重命名为 `AI标题_年-月-日`(如 `深度解析DeepSeek_26-3-1.md`),勾选"保留原标题"则保持原文件名
3. **筛选页新增"数据源"下拉** —— 选择"爬取数据(DataPC)"或 AI 分类结果(AI-高质量/广告/可疑,目录存在时自动出现),可对 AI 分类后的文章再次按热度/字数筛选(已适配 AI 重命名后的新文件名)
4. **列表页字数粗过滤** —— 设置最低正文字数后,列表页预览字数不足的文章不再进入详情页,节省爬取时间(详情页仍有精确过滤)
5. **灰色提示文字自动换行** —— 界面所有灰色提示文字会随窗口宽度自动换行,无需手动拉宽窗口
6. **导出日志按钮** —— 日志区右上角"导出日志",将本次运行日志保存为 txt 文件
7. **打开文件夹按钮** —— 爬取页与筛选页日期行旁新增"打开月份文件夹":按所选日期一键打开 `DataPC\<博主>_<ID>\<年份>年\<月份>\`(不存在时自动回退到博主目录/DataPC 根);日志区右侧"打开数据目录"直达 DataPC 根目录,无需层层点开资源管理器
8. **筛选结果按博主分目录** —— 筛选输出改存到 `筛选\<博主名>_<ID>\`,多位博主的筛选结果不再混杂;并新增日期范围校验(开始日期晚于结束日期时直接提示,避免无效区间导致"未找到文件")
9. **文件夹操作小按钮** —— ① 导出日志默认打开程序所在目录;② AI筛选页进度条旁"打开输出文件夹"直达筛选目录;③ 筛选页新增"筛选完成后自动打开输出文件夹"(默认不勾选)与"打开当前筛选任务文件夹"按钮(按当前表单条件定位) 

## 使用说明

- 仅发布 GUI 版本(weibo-md-exporter-GUI.exe)
- AI功能为可选:不配置 API 则完全不影响原有爬取/筛选功能
- API Key 只保存在本地 ai_config.json,请自行妥善保管
- 分类规则(事后分类): 广告概率≥广告阈值→广告; ≥可疑下界→可疑; 高质量可信度≥阈值→高质量; 其余→其他低质量

## 本版本保留的功能(与 V0.3.3 相同)

- 博主记录文件(下拉框自动填充ID、打开/刷新按钮、自动记录新博主)
- 本地数据筛选(转评赞/正文字数,热度TOP/字数TOP文件夹,同步复制媒体,设置记忆)
- 最低字数过滤;开始日期切月重置1日;跳过已爬取文章;增量补下载媒体
- 日期选择器(10年前~明年);图片/视频大图下载;docx导出(宋体/嵌入图片)
- 年份\月份目录归档;自动识别类名;爬取间隔自定义;GUI记住全部设置

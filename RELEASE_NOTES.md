# V0.4.1 更新内容

本版本为**修复版**(基于 V0.4.0),包含以下改动:

## 本版本修复内容

1. **AI重命名文件排序问题** —— AI总结重命名格式由 `<AI标题>_<年-月-日>` 调整为 **`<年-月-日>_<AI标题>`**(如 `26-1-22_深度解析.md`)。日期放在文件名开头后,资源管理器按"名称"排序(点列头升序/降序)即按日期排序;筛选功能已同步适配,新旧格式均能识别
2. **扫描日期解析增强** —— 支持三种文件名(日期开头 / 博主_日期_ID / 标题_日期),并优先取文件名开头的真实日期,避免标题中带有日期的文字干扰

## 本版本支持的功能(与 V0.4.0 相同)

- AI实时判断(爬取页,高质量可信度低于阈值不导出不下载媒体)
- AI筛选页签:API设置/阈值/提示词/事后AI分类(保留原文件)/AI总结(日期_标题重命名)
- 筛选页数据源下拉(AI分类结果可再次筛选);筛选结果按博主分目录
- 选择AI分类结果后,原文件自动保留在 DataPC;断点续跑与"清空进度"
- 最低字数列表页粗过滤;灰色提示自动换行;日期范围校验
- 打开月份文件夹/数据目录/筛选结果文件夹;导出日志(默认程序目录)
- 博主记录文件、本地热度/字数筛选、增量补下载媒体、docx导出等此前全部功能

## 使用说明

- 仅发布 GUI 版本(weibo-md-exporter-GUI.exe)
- 已用 V0.4.0 分类过的文件(标题_日期格式)仍可正常筛选;如需统一为"日期_标题"格式,请"清空进度"后重新执行AI分类(会再次消耗token)

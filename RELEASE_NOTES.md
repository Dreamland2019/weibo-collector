# V0.2.3 更新内容

本版本为**改进版**(基于 V0.2.2),包含以下改动:

## 本版本改进内容

1. **GUI 记住全部设置** —— 博主昵称、微博ID、起止日期、无头模式开关、用户数据目录、是否保留浏览器、是否只收集ID、爬取间隔、是否下载图片/视频、导出格式、类名覆盖等设置,全部在关闭程序后保存,下次打开自动恢复,无需重新填写。

## 本版本支持的功能(与 V0.2.2 相同)

- 自动识别微博页面类名;爬取间隔自定义;年月日选择器(自动处理大小月/闰年)
- 年份\月份目录归档(txt 入年份夹);图片/视频可选下载(大图)
- 导出格式可选(Markdown / Word docx,docx 支持嵌入图片、宋体)
- 修复时间范围过滤、图片抓取范围、无头模式登录等已知问题

## 使用说明

- GUI 所有设置自动记忆;首次使用日期为空,选择后自动保存
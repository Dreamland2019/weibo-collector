# V0.2.1 更新内容

本版本为**修复版**(基于 V0.2.0),包含以下改动:

## 本版本修复的问题

- **修复图片抓取范围过宽的问题**:之前会在全页范围查找图片,导致无图微博也抓取到正文卡片之外的图片(如右侧用户信息区的头像背景图)。现改为**只在正文卡片内**查找配图,无图微博不再误抓。
- **修复抓取到的配图是缩略图的问题**:之前抓到的配图是 orj360 缩略图(约19KB),放大后文字模糊不清。现自动将缩略图标记(orj360/mw690 等)替换为 large,抓取**大图**(约300KB~1.4MB),图片中的文字清晰可读。
- 过滤视频播放器的 SVG 占位图(data: 协议),避免误识别为配图。

## 本版本支持的功能(与 V0.2.0 相同)

1. **爬取间隔可自定义** —— 可在高级设置中调整每条微博之间的等待秒数范围(默认 3~8 秒)
2. **日期选择器** —— GUI 年/月/日下拉选择(自动处理大小月与闰年)
3. **改进文件存放逻辑** —— 文章按 年份年\月份\ 归档(如 2026年\4月\),微博ID列表(txt)放入年份文件夹
4. **可选下载图片/视频** —— 配图以**大图**形式下载到本地,并在 Markdown 中引用本地文件
5. **导出格式可选** —— 支持导出为 Word(docx)或 Markdown

## 使用说明

- 重新爬取之前抓过的微博时,勾选"下载图片"即可获得大图版本;已爬取的旧文件中的缩略图不会自动更新,可重新爬取覆盖。
# V0.6.0 更新内容

本版本为**功能大版本**(基于 V0.5.2):补上产品闭环的"最后一公里"——把爬取/筛选/AI 分类的成果一键导入 Obsidian(或任意 Markdown 笔记库)阅读沉淀。

## 本版本新增

1. **全新「导出」页签**(第 4 页签:爬取 / 数据筛选 / AI分类 / 导出)+ 三个页签与日志区的**快捷入口**(自动跳转并预填上下文)
2. **两种导入目标**:`obsidian`(写入 vault;「选vault」自动检测本机 Obsidian 知识库)与 `markdown-folder`(任意目录,通用,Logseq 等 md 软件也能读)
3. **笔记渲染**:13 字段属性(frontmatter:标题/作者/来源/源ID/日期/词条/类别/可信度/转评赞/导入时间/标签)+ 短日期文件名(26-8-8_标题.md)+ **关键信息引用块**(URL/发布时间/词条/字数 与 转评赞/保存时间,可关)+ 标签(博主/类别/年月/话题)
4. **配图管理**:图片复制到 `微博长文/附件/<博主>/<年>/<月>/` 并自动改写引用(离线可看);视频保留网络链接
5. **防重复(幂等)**:按笔记内 `weibo_id + publish_date` 识别(改名/移动/删除均安全);已存在默认跳过;可选"覆盖已存在笔记"/同名不同源"另存加后缀";单篇失败不中断,完成报告含 新增/跳过/更新/冲突/失败
6. **可选 CSS 片段**:导入时写入 `.obsidian/snippets/weibo-notes.css`,启用后**阅读视图默认隐藏属性面板**(Ctrl+P → Toggle properties 随时查看);正文直接可读
7. **设置记忆**:导出页专属设置(博主/范围/来源/目标/各选项)重启后保留

## 架构决策(见 docs/adr/)

- **不做 Obsidian 原生插件**:技术栈断档、双份维护;Obsidian 本质是"磁盘上的 md 文件夹",写文件即导入(ADR-0001)
- **独立导入 + 目标适配器**:核心渲染标准 md+frontmatter;`obsidian`/`markdown-folder` 为首批目标,Logseq/思源/Joplin 为后续候选;云端笔记 API(Notion/语雀/飞书)明示不做
- **幂等键 = 目标笔记内 weibo_id+publish_date**,而非程序状态记录:用户在 Obsidian 里改名/移动/删除都不会误判(ADR-0002)

## 数据兼容说明

- 旧版本(v0.5.2 及更早)的 DataPC/AI分类 数据直接可用,无需迁移
- 导入前可"预扫描"显示将新增/跳过篇数;已导入过的笔记重导会跳过

## 版本记录

- v0.5.2(修复版):微博状态记录;AI判定复用;更新转评赞(月份范围);实时/事后防重复;优雅停止;单实例等
- v0.5.1(修复版):docx 导出修复;爬取前博主校验;筛选目录命名等
- 更早版本见 README

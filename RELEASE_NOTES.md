# V0.5.1 更新内容

本版本为**修复版**(基于 V0.5.0),修复首轮公开测试(测试清单 §1.1~§4)发现的 8 个问题并优化 1 处:

## 本版本修复的问题

1. **docx 导出失败(高)** —— 报 `name 'self' is not defined`,docx 格式无法导出任何文章;已修复(静态方法内错误使用 self)
2. **博主记录追加不换行(高)** —— 记录文件末行无换行(如手动编辑过)时,新博主会被拼接到上一行,导致记录解析错误;追加前自动补换行
3. **错误博主ID提示误导(中)** —— ID 不存在时误报"该时间段没有发表微博";现在**爬取前**访问主页校验:**ID 不存在 → 明确提示"博主不存在"**;**昵称与 ID 不匹配 → 开始前提示并阻止爬取**(附主页链接)
4. **校验不通过的博主仍写入记录文件(中)** —— 此前爬取开始前就追加博主记录;现在改为**校验通过并成功爬取后**,按浏览器实际抓取到的用户名追加记录;AI 分类页同样调整为分类成功后写入
5. **AI配置未点"保存配置"无法生效(低)** —— 爬取页勾选"启用AI实时判断"后,若 AI 页签已填 Key 但未点保存,自动保存一次配置
6. **AI分类后数据源下拉需重启(低)** —— AI 分类完成即刷新"数据源"下拉,切换页签到"数据筛选"时也自动刷新,无需重启
7. **筛选结果混杂(低)** —— 不同数据源(DataPC/AI分类)与不同勾选指标(如仅转发)的筛选结果输出到同一文件夹;现在文件夹名含 **日期范围(年份省去20)+ 数据源 + 指标**(如 `26-08-01~26-08-31_热度TOP10_AI_高质量_转发`),各组合互不混杂;旧的完整年份文件夹仍可被"打开当前筛选任务文件夹"识别
8. **未勾选指标时按日期排序(低)** —— 现在默认按 转发+评论+点赞 之和排序(界面与核心双层兜底)

## 本版本新增/优化

1. **AI分类时复制媒体(可选项)** —— 实时AI分类结果(重命名副本)默认**连带复制图片/视频**到 `AI分类/` 目录,修复此前副本图片引用损坏的问题;取消勾选"分类时复制媒体"时,副本中的媒体引用改为**原微博网络链接**(不占用本地空间,需联网查看);docx 图片内嵌不受影响
2. **无头模式登录后自动恢复(优化)** —— 无头模式检测到未登录时弹出有头窗口登录,登录成功后自动关闭窗口并恢复无头模式继续,不影响用户使用电脑

## 数据兼容说明

- 筛选文件夹:新命名规则(年份省略"20")与旧命名(完整年份)并存,程序均可识别
- 博主记录文件:损坏的记录(已有拼接行)不会自动修复,如遇解析异常可手动编辑 `DataPC/博主记录.txt`

## 版本记录

- v0.5.0(功能版):程序更名;AI分类与数据筛选分目录;已有月份下拉;任务互斥;实时AI总结重命名;URL超链接;ⓘ悬浮提示;窗口大小记忆
- 更早版本见 README

# V0.2.0 更新内容

本版本为**功能更新版**,包含以下改动:

## 本版本支持的功能

1. **爬取间隔可自定义** —— 可在高级设置中调整每条微博之间的等待秒数范围(默认 3~8 秒),并提示不要设置过短以避免触发微博风控
2. **日期选择器** —— GUI 改为年/月/日下拉选择(日的选项随年月自动调整,自动处理大小月与闰年),不再需要手动输入日期
3. **改进文件存放逻辑** —— 文章按发布年份和月份归档,目录结构为 `DataPC\<博主>_<ID>\<年份>年\<月份>\`(如 `2026年\4月\`);微博ID列表(txt)放入对应的年份文件夹(如 `2026年\`)。无论爬取时间范围多长(数月)或多短(两天),文章都按发布月份自动归入对应文件夹
4. **可选下载图片/视频** —— 勾选后可将微博文章中的图片、视频下载到本地,并在 Markdown 中引用本地文件
5. **导出格式可选** —— 支持导出为 Word(docx)或 Markdown

## 本版本修复的问题

- 修复无头模式(headless)下未登录时无法登录的问题:自动切换为有头模式弹出浏览器供用户扫码登录
- 修复微博正文图片无法提取的问题(图片不在正文文本容器中,改为全页识别)
- 修复图片下载后 Markdown 中引用路径的兼容性(统一为正斜杠)
- 修复时间范围过滤失效的问题:微博页面首次加载时 URL 时间参数未生效,会抓到范围外的微博;现已在收集前自动点击页面"搜索"按钮,确保只抓取所选日期范围内的微博

## 使用说明

- GUI 用户:直接在"高级设置"中勾选/填写新选项
- 命令行用户:新增 `--min-interval/--max-interval`、`--download-images/--download-videos`、`--format md|docx` 参数
- 输出目录结构示例:
  ```
  DataPC\卢诗翰_3276099007\
  └─ 2026年\
     ├─ 卢诗翰_3276099007_2026-04-01_2026-04-30.txt   ← 微博ID列表
     └─ 4月\
        ├─ 卢诗翰_26-4-7_xxx.md / .docx                ← 每条微博
        └─ images\                                     ← 下载的图片(可选)
  ```

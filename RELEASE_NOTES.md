# V0.2.2 更新内容

本版本为**改进版**(基于 V0.2.1),包含以下改动:

## 本版本改进内容

1. **docx 导出支持嵌入图片** —— 导出 Word 文档时,已下载的图片会按顺序直接嵌入文档中,无需再到图片文件夹查找;未下载图片时,文档中会写入图片链接并提示"勾选下载图片后重新导出"。视频无法嵌入 docx,文档中会写入视频链接,并提示视频文件位置。
2. **docx 全文使用宋体** —— 导出的 Word 文档默认字体改为宋体(含中文),更适合阅读与打印。
3. **记住上次选择的日期** —— GUI 会保存上次爬取的起止年月日,下次打开自动填入;首次使用则日期为空,由用户自行选择。

## 本版本支持的功能(与 V0.2.1 相同)

- 自动识别微博页面类名;爬取间隔自定义;年月日选择器(自动处理大小月/闰年)
- 年份\月份目录归档(txt 入年份夹);图片/视频可选下载(大图)
- 导出格式可选(Markdown / Word docx);修复时间范围过滤、图片抓取范围等已知问题

## 使用说明

- 导出 docx 时如需图片嵌入文档,请在爬取时勾选"下载图片"
- GUI 首次打开日期为空,选择后自动记住,下次打开自动填入
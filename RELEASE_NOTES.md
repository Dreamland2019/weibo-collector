# V0.5.0 更新内容

本版本为**功能大版本**(基于 V0.4.1),程序正式更名为 **微博长文收集记录整理器**(exe 为 `weibo-collector-GUI.exe`):

## 本版本新增与改进

1. **程序更名** —— 窗口标题改为"微博长文收集记录整理器 v0.5.0"(名称带版本号);exe 文件名改为 `weibo-collector-GUI.exe`(仓库名稍后同步)
2. **AI分类与数据筛选分目录** —— AI 分类结果输出到 `AI分类/AI_博主_类别/`,热度/字数筛选结果仍在 `筛选/<博主>_<ID>/`,互不混杂;筛选页"数据源"自动扫描两处目录(旧数据兼容)
3. **已有月份下拉(数据筛选)** —— 自动读取该博主已爬取的月份,"从/到"起止月选择后自动填充日期范围(支持多月),手动日期仍可微调
4. **任务互斥** —— 爬取与AI分类互斥(防止同时读写数据);数据筛选可与爬取并行
5. **实时AI总结重命名** —— 爬取页勾选"AI总结并重命名标题"后,AI判定通过的文章自动复制重命名到 `AI分类/高质量/<日期_标题>.md`,原文件保留
6. **URL 超链接** —— Markdown 输出 `[微博原文链接](url)`,docx 输出可点击蓝色超链接,阅读时直接点开看原文/评论区
7. **ⓘ 悬浮提示** —— 长提示收进行尾"ⓘ"图标,鼠标悬停显示(分号自动换行);短提示与重要提示(爬取间隔/筛选说明/高质量阈值)直接显示
8. **窗口大小记忆** —— 关闭时记录窗口尺寸,下次打开沿用(含最大化状态)
9. **AI分类防重复增强** —— 分类进度记录移到博主数据目录,换输出目录也不重复分类;完成提示"已跳过 N 篇已分类"
10. **界面细节** —— 数据格式/输出文件夹独立成行;日志区四个操作按钮竖排;窗口内容随宽度自适应,灰色提示不再挤在行尾

## 数据迁移说明

V0.4.1 及以前 AI 分类结果位于 `筛选/` 目录,新版自动兼容两处目录;如需整理文件结构,可把 `筛选/AI_*` 目录移动到 `AI分类/` 下(筛选页数据源两处都能识别)。

## 版本记录

- v0.4.1(修复版):AI重命名"日期_标题"格式;文件名扫描兼容
- v0.4.0(功能版):AI智能筛选(实时可信度判断/事后AI分类/AI总结/筛选数据源)
- 更早版本见 README

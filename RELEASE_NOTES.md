# V0.1.1 更新内容

本版本为**修复版**(基于 V0.1.0),包含以下改动:

## 本版本修复的问题

- **修复无头模式(headless)下无法登录的问题**:首次启动未登录且勾选"无头模式"时,由于浏览器窗口不可见,用户无法扫码登录。现改为:检测到未登录时自动重启为有头模式,弹出浏览器窗口供用户扫码登录,登录后继续自动爬取。

## 本版本支持的功能(与 V0.1.0 相同)

- 自动识别微博页面类名,类名变化无需手动改代码(极端情况可手动输入)
- GUI 图形界面 / 命令行 两种使用方式
- 按博主 + 时间范围收集原创长文微博,导出 Markdown
- 自动跳过转发微博;重跑同一时间段自动覆盖旧文件
- 博主在选定时间段无微博(如禁言)时给出友好提示
# V0.3.2 更新内容

本版本为**优化版**(基于 V0.3.1),包含以下改动:

## 本版本优化内容

1. **博主记录文件** —— DataPC 目录下新增"博主记录.txt",爬取页与筛选页的博主昵称改为**下拉框**,选中自动填充微博ID;点击"打开博主记录"按钮可用记事本编辑该文件;爬取新博主后自动写入记录
2. **最低正文字数可自定义** —— 高级设置新增"最低正文字数",正文低于该字数的文章不导出(0=不限制)
3. **开始日期优化** —— 切换开始日期的月份时,日期自动重置为 1 日(结束日期保持原逻辑)
4. **筛选同步复制媒体** —— 筛选出的文章,其对应的图片/视频会自动复制到筛选结果文件夹的 images/ videos/ 子目录
5. **筛选文件夹区分热度/字数** —— 按转评赞筛选输出到 ..._热度TOP<N> 文件夹,按字数筛选输出到 ..._字数TOP<N> 文件夹
6. **筛选功能记住设置** —— 筛选页的博主/日期/排序方式/指标勾选/篇数/格式等设置会自动保存,下次打开自动恢复
7. **增量补下载媒体(方案A)** —— 开启"跳过已爬取文章"后,若某篇文章已存在但图片/视频缺失(如之前只爬文章没下图片),会自动重新抓取详情**仅补下载媒体**,不重写文章文件;已确认无图的文章会打标记,下次不再重复检查(避免无图文章反复抓详情)

## 本版本支持的功能(与 V0.3.1 相同)

- 本地数据筛选(转评赞之和 / 正文字数,TOP-N,序号重命名,筛选说明.txt)
- 日期选择器(年份 10 年前~明年,可手动输入;开始日期切月重置为1日)
- 跳过已爬取文章(按微博ID去重,默认勾选);最低字数过滤
- 常用选项(下载图片/视频、导出格式、无头模式)位于任务参数区
- 图片/视频大图下载;docx 导出(宋体、嵌入图片);年份\月份目录归档
- 自动识别页面类名;爬取间隔自定义;GUI 记住全部设置(含筛选页)

## 使用说明

- 仅发布 GUI 版本(weibo-md-exporter-GUI.exe)
- 博主记录文件位于 DataPC/博主记录.txt,格式: 微博ID 博主昵称
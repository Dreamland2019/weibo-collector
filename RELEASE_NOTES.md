# V0.5.2 更新内容

本版本为**修复+增强版**(基于 V0.5.1),核心是引入**微博状态记录**并新增"更新转评赞",同时修复多轮测试反馈的问题:

## 本版本新增

1. **微博状态记录** —— 每个博主一个 `DataPC/<博主>_<ID>/微博状态.json`,逐篇记录:发布时间、是否导出、文件名、AI判定分数与时间、转评赞数值与最后检查时间、媒体有无
2. **AI判定复用(修复"AI评分漂移")** —— 之前被AI判定过的文章重跑时会**复用上次分数**,不再重复抓详情+调用大模型(省tokens、结果稳定);新选项"重跑时重新AI判断"可强制重判
3. **更新转评赞** —— 选择该博主**月份范围(或全部)**,逐篇抓详情**只刷新 md/docx 中的转发/评论/点赞数字**(正文与AI分类不动);建议月末/季末/年末各跑一次;支持中途停止(已完成部分保留);执行前显示篇数与预计耗时
4. **实时+事后分类防重复** —— 已通过"实时AI总结重命名"复制到 AI分类 的文章,事后分类时**默认跳过**(避免内容相同但标题不同的重复文件);可勾选"包含已实时分类的文章"重新分类

## 本版本修复/优化

1. **优雅停止** —— "停止"按钮不再只是提示:当前微博处理完即干净结束,浏览器正常释放,结果提示"任务已停止(部分完成)"
2. **单实例** —— 同一时间只允许一个程序实例(文件锁,进程退出自动释放),避免双开争用数据目录
3. **无头模式与"完成后保留浏览器"互斥** —— 同时勾选会导致浏览器在后台残留、影响下次爬取
4. **无媒体自动标记** —— 确认无图/无视频时立即写 `.nomedia` 标记,重跑不再反复抓详情(日志不再出现误导性的"补下载媒体")
5. **类名体验** —— 非法类名(如误填"1")不再打印大段堆栈;程序自动探测到正确类名后,界面输入框与设置自动同步,不再用旧错误值覆盖
6. **AI分类页布局** —— "已有月份"下拉(与筛选页一致);选项拆两行;ⓘ提示独立放在各选项旁
7. **事后AI分类提示** —— 该时间段无文章/全部已实时分类时给出明确提示

## 数据兼容说明

- 旧版本数据无需手动处理:程序已内置兼容(状态字段缺省为"未知",按保守策略处理);也可运行 `data_maintenance.py migrate` 补齐状态记录(重复运行安全)
- 状态文件带版本号(`format_version`),未来新增字段不影响老数据

## 版本记录

- v0.5.1(修复版):docx导出失败/博主记录换行;爬取前校验;AI配置自动保存;数据源即时刷新;AI副本复制媒体;筛选目录命名
- v0.5.0(功能版):程序更名;AI分类与数据筛选分目录;已有月份下拉;任务互斥;实时AI总结重命名;URL超链接;ⓘ悬浮提示;窗口大小记忆
- 更早版本见 README
